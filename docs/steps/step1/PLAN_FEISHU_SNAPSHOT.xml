<title>step1</title><h2>Step 1：构造一个天然存在 candidate collapse 的环境</h2><p>不要选普通 benchmark。</p><p>因为普通任务：</p><pre caption="&#xA;" lang="Plain Text"><code>candidate 都不同</code></pre><p>无法验证。</p><p>需要任务存在：</p><blockquote><p>多条路径开始相似，但在某个关键决策点后分裂。</p></blockquote><p>推荐：</p><h2>Benchmark A：Push-T / PushCube（首选）</h2><p>原因：</p><p>简单、有多模态策略。</p><p>任务：</p><p>机器人需要把物体推到目标区域。</p><p>设计：</p><p>加入：</p><ul><li> 障碍物； </li><li> 狭窄通道； </li><li> 两条可行路径； </li><li> 一个错误吸引区域。 </li></ul><p>例如：</p><pre caption="&#xA;" lang="Plain Text"><code>          goal<br/><br/>     /-----------\<br/>start</code></pre><p>存在：</p><ul><li> 左绕； </li><li> 右绕。 </li></ul><p>但是 diffusion policy 初始采样：</p><p>100 个 candidate：</p><p>前 40%：</p><p>全部：</p><pre caption="&#xA;" lang="Plain Text"><code>向右移动</code></pre><p>之后：</p><p>少量进入：</p><pre caption="&#xA;" lang="Plain Text"><code>绕障碍</code></pre><p>大部分：</p><pre caption="&#xA;" lang="Plain Text"><code>撞墙</code></pre><p>形成：</p><pre caption="&#xA;" lang="Plain Text"><code>candidate tree<br/><br/>          root<br/>            |<br/>            |<br/>        bottleneck<br/>          /    \<br/>       good    bad</code></pre><hr/><h1>Step 2：定义 candidate genealogy</h1><p>这是核心。</p><p>不要只看最终 trajectory。</p><p>需要保存：</p><p>每个 candidate：</p><pre caption="&#xA;" lang="Plain Text"><code>candidate_id<br/><br/>parent_id<br/><br/>generation_step<br/><br/>latent_state_t<br/><br/>action_prefix<br/><br/>predicted_success</code></pre><p>形成：</p><pre caption="&#xA;" lang="Plain Text"><code>candidate tree</code></pre><p>例如：</p><pre caption="&#xA;" lang="Plain Text"><code>          root<br/><br/>        /      \<br/>      A          B<br/><br/>    / | \      / | \<br/><br/>   A1 A2 A3 B1 B2 B3</code></pre><hr/><h1>Step 3：定义 bottleneck detection</h1><p>先不要追求 fancy。</p><p>使用三个 baseline。</p><hr/><h2>Baseline 1：No split</h2><p>普通 sampling：</p><pre caption="&#xA;" lang="Plain Text"><code>sample N candidates<br/>select best</code></pre><hr/><h2>Baseline 2：Uniform split</h2><p>固定：</p><p>例如：</p><pre caption="&#xA;" lang="Plain Text"><code>每 10 diffusion steps<br/>增加 diversity</code></pre><p>类似：</p><pre caption="&#xA;" lang="Plain Text"><code>所有地方平均探索</code></pre><hr/><h2>Baseline 3：Random split</h2><p>随机选择一个 candidate generation step split。</p><hr/><h2>Proposed：</h2><p>Bottleneck-local split</p><p>定义：</p><p>候选之间 divergence：</p><p>Dt=Var(a1:t)</p><p>或者：</p><p>latent variance：</p><p>Dt=Var(zt)</p><p>找到：</p><p>t∗=argmax(future failure separation−current diversity)</p><p>直觉：</p><p>找到：</p><blockquote><p>当前大家还一样，但未来结果已经开始分叉的位置。</p></blockquote><hr/><h1>Step 4：Split operator</h1><p>不要复杂。</p><p>第一版：</p><p>在 bottleneck:</p><p>原：</p><pre caption="&#xA;" lang="Plain Text"><code>latent z</code></pre><p>生成：</p><pre caption="&#xA;" lang="Plain Text"><code>z + epsilon1<br/><br/>z + epsilon2<br/><br/>z - epsilon1<br/><br/>z - epsilon2</code></pre><p>保持：</p><p>总 candidate 数固定。</p><p>例如：</p><p>baseline：</p><p>100 candidates</p><p>你的：</p><p>50 candidates 正常</p><p>50 candidates 在 bottleneck split</p><hr/><h1>Step 5：实验指标</h1><p>不要只看 success。</p><p>必须验证机制。</p><h2>Main metric</h2><h3>Success under fixed compute</h3><p>例如：</p><p>固定：</p><pre caption="&#xA;" lang="Plain Text"><code>100 forward calls</code></pre><p>比较：</p><sheet sheet-id="mhRBro" token="CLyjsSmSZhxQxdtLZiZc01tZnMd"></sheet><hr/><h2>Mechanism metric</h2><p>必须有。</p><h3 seq="auto" seq-level="auto">Bottleneck localization accuracy</h3><p>定义：</p><p>真实 bottleneck：</p><p>人工标注：</p><pre caption="&#xA;" lang="Plain Text"><code>第一次进入不可恢复错误路径的位置</code></pre><p>预测：</p><pre caption="&#xA;" lang="Plain Text"><code>t*</code></pre><p>指标：</p><p>error：</p><p>∣t∗−tgt∣</p><hr/><h3 seq="2" seq-level="auto">Candidate diversity efficiency</h3><p>不是看 diversity 大。</p><p>看：</p><pre caption="&#xA;" lang="Plain Text"><code>diversity / compute</code></pre><p>例如：</p><p>number of samplessuccessful modes</p><hr/><h3 seq="3" seq-level="auto">Mode discovery</h3><p>最终找到多少不同成功模式。</p><p>例如：</p><p>100 sample：</p><p>普通：</p><pre caption="&#xA;" lang="Plain Text"><code>100个都是右侧失败</code></pre><p>你的：</p><pre caption="&#xA;" lang="Plain Text"><code>40右侧失败<br/>30左侧成功<br/>30中间探索</code></pre><hr/><h1>Step 6：必须做的 ablation</h1><p>至少：</p><h2>A1</h2><p>No bottleneck detection</p><p>随机 split。</p><h2>A2</h2><p>正确 bottleneck + random split operator</p><h2>A3</h2><p>wrong bottleneck</p><h2>A4</h2><p>full resampling</p><h2>A5</h2><p>增加 sample 数量</p><p>证明：</p><p>不是：</p><blockquote><p>sample 更多赢了。</p></blockquote><p>而是：</p><blockquote><p>split location 对。</p></blockquote><p></p>
