<title>TASK: R142-FP-11 Stage-R — trajectory-axis revalidation</title><h2>CONTEXT</h2><p>Prior work is in <code>mikasaTu/R142-FP-11-Bottleneck-Local-Candidate-Split</code>.<br/>Read <code>docs/steps/context/ORIGINAL_IDEA.md</code> for the frozen hypothesis.<br/>Stage-1 (toy) and Stage-2A (denoising axis on PushT) are CLOSED. Do not extend,<br/>rerun, or cite them as support. Their known defects:</p><ul><li>Stage-1 benchmark hard-codes the location effect in <code>rollout()</code> control flow;<br/>its detector has a 75.7% false-positive rate on a no-spike environment.</li><li>Stage-2A branched along the DDPM denoising axis, not the trajectory axis, used<br/>an 8-control-step ceiling-bound outcome, and never verified the precondition.</li></ul><p>Start a new track <code>stage-r/</code> in the same repo. Reuse only the replay/snapshot,<br/>genealogy-logging, PAI-launcher and SHA-256 manifest infrastructure.</p><h2>OBJECTIVE</h2><p>Test the ORIGINAL hypothesis on the correct axis:</p><blockquote><p>When a candidate family collapses onto a shared failing prefix, does there<br/>exist an earliest control step t* at which concentrating a fixed diversity<br/>budget beats uniform/random split locations and beats plain best-of-N, as<br/>measured by EVENTUAL episode success?</p></blockquote><h2>HARD CONSTRAINTS</h2><ol><li seq="1">Dependent variable is EVENTUAL episode success (run to termination). Any<br/>short-horizon proxy is a secondary metric only. Never gate on one.</li><li>Branch location is a CONTROL STEP in the trajectory. Never a denoising step.</li><li>Freeze every protocol file BEFORE inspecting outcomes. Commit it first.</li><li>Effect-size thresholds are NOT hand-picked numbers. Compute a permutation<br/>null (&gt;=1000 shuffles) and a null-control task, set every threshold at the<br/>95th percentile of that null, and commit the threshold before unblinding.</li><li>Every phase runs a POSITIVE CONTROL. A negative result without a passing<br/>positive control is <code>PIPELINE_INVALID</code>, not a scientific finding.</li><li>Aggregate-first statistics. Primary test on the task-level pooled curve with<br/>episode as the resampling unit (paired bootstrap, 10000 replicates).<br/>Per-episode prevalence is secondary. Do not gate on per-unit significance<br/>with small M.</li><li>Report all negative controls and flat cases. Never filter examples.</li><li>Compute accounting in policy forward passes (primary) and env steps<br/>(secondary). Report branch counts and slack per strategy.</li><li>Completion means a persisted <code>COMPLETED_*.json</code> with SHA-256 verification.<br/>Queued / Running / first-work / one finished shard is NOT a result.</li></ol><h2>PROHIBITED</h2><ul><li>Fabricating, interpolating, or resampling any trajectory, state, or outcome.</li><li>Substituting a cheaper proxy for eventual success in any gate.</li><li>Selecting episodes, snapshots, or examples after seeing descendant outcomes.</li><li>Relaxing, rewriting, or reinterpreting a committed threshold after unblinding.</li><li>Using latent/z pairwise distance as a detector signal (already falsified).</li><li>Advancing past a CHECKPOINT without explicit human approval.</li><li>Reporting a partial run as complete, or claiming a gate passed without the<br/>persisted artifact that proves it.</li></ul><hr/><h2>PHASE E — engineering gates (blocking)</h2><p>Pin one policy + checkpoint (revision + weights SHA-256) and one simulator<br/>stack (exact commit + package versions). Then prove:</p><ul><li>E1 environment/checkpoint manifest persisted.</li><li>E2 with instrumentation disabled, the path is bit-identical to the original.</li><li>E3 state restore -&gt; same action -&gt; next state error &lt;= 1e-9.</li><li>E4 restore covers simulator state AND policy observation-history buffer AND<br/>action-chunk queue AND all RNG streams. Prove each component separately by<br/>ablating it and showing divergence. THIS IS THE MOST COMMON SILENT FAILURE.</li><li>E5 changing only branch RNG changes the executed actions (non-degenerate).</li><li>E6 baseline success rate in [0.25, 0.75] and final-progress distribution not<br/>piled at the upper bound.</li></ul><p>Any failure -&gt; <code>STAGE_R_ENGINEERING_INCOMPLETE</code>. Fix or change checkpoint. Do<br/>not proceed to science.</p><p>Write <code>docs/stage_r/ENGINEERING_GATES.md</code> + <code>results/stage_r/gates/</code>.</p><hr/><h2>PHASE 0R — precondition screen and task selection (observational only)</h2><p>NO intervention in this phase.</p><p>Candidate tasks: LIBERO suites (spatial / object / goal / 10) and RoboTwin.<br/>For each task: E=16 initial states, N=32 independent full rollouts each.</p><p>Compute per task:</p><ol><li seq="1">Overdispersion <code>rho = Var(p_e) / [p_bar(1-p_bar)/N]</code>, plus the fraction of<br/>episodes with <code>p_e &lt;= 1/N</code>.</li><li>Shared failing prefix: for all-fail episodes, per-step pairwise divergence<br/>D(t) over end-effector and object pose, workspace-normalized; first crossing<br/>t_div. Also hierarchical clustering of executed action sequences -&gt; step at<br/>which the merge tree splits into &gt;=2 clusters.</li><li>Mode multiplicity: cluster mid-trajectory behavior of successful rollouts;<br/>count stable modes.</li><li>Ceiling check per E6.</li></ol><p>RETAIN a task only if: rho &gt;= 3 AND &gt;=25% episodes with p_e &lt;= 1/N AND median<br/>t_div &gt;= 10% of episode length AND modes &gt;= 2 AND E6 holds.</p><p>Keep at most 3 tasks. If zero tasks pass, write the report and STOP.</p><p>Deliverables:</p><ul><li><code>docs/stage_r/PHASE0R_PROTOCOL.md</code> (committed before running)</li><li><code>reports/stage_r/PHASE0R_REPORT.md</code> with the full table for ALL candidate<br/>tasks including rejected ones</li><li>raw per-rollout records under <code>results/stage_r/phase0r/</code></li></ul><h3>&gt;&gt;&gt; CHECKPOINT 1 — STOP HERE &lt;</h3><p>Post the Phase-0R table and the retained task list. Wait for explicit human<br/>approval before Phase 1R. Do not start Phase 1R work of any kind.</p><hr/><h2>PHASE 1R — recoverability profile along the trajectory axis</h2><p>Only for tasks approved at Checkpoint 1.</p><p>Per task: select 12 episodes descendant-blind by deterministic SHA-256 rank,<br/>stratified 4 failed / 4 marginal / 4 successful from the Phase-0R baseline.<br/>Branch grid: 10 control steps at episode-length deciles.<br/>Per (episode, t): restore, resample M=16 action chunks with independent RNG,<br/>execute, then CONTINUE WITH THE FROZEN POLICY TO EPISODE TERMINATION.<br/>Split RNG into disjoint calibration and held-out streams.</p><p>Primary: <code>R(t) = P(eventual success | branch at t)</code>, pooled per task.<br/>Derived: location sensitivity <code>max R - min R</code>; largest adjacent cliff;<br/>commit point t_c = earliest t where R(t) &lt; (Rmax+Rmin)/2.</p><p>Run all three controls in the SAME pipeline:</p><ul><li>POSITIVE CONTROL: a variant with a verified irreversible commitment.<br/>Construct it explicitly and document the construction.</li><li>NULL CONTROL: a single-mode, highly recoverable task.</li><li>PERMUTATION NULL: &gt;=1000 within-episode branch-label shuffles per task.</li></ul><p>Threshold: 95th percentile of the permutation null. Commit it before unblinding.</p><p>Decision:</p><ul><li>positive control below threshold -&gt; <code>PIPELINE_INVALID</code>. Stop, fix tooling.</li><li>positive control passes, natural below threshold -&gt;<br/><code>NO_TRAJECTORY_BOTTLENECK_ON_&lt;policy&gt;_&lt;task&gt;</code>. This is a valid negative<br/>result. Write it up fully. Do not proceed to Phase 2R.</li><li>positive control passes, natural at/above threshold -&gt; proceed.</li></ul><p>Deliverables: frozen protocol, <code>reports/stage_r/PHASE1R_REPORT.md</code>, per-task<br/>R(t) curves, full genealogy, all control results, compute accounting.</p><h3>&gt;&gt;&gt; CHECKPOINT 2 — STOP HERE &lt;</h3><p>Post R(t) curves for natural + positive + null controls, the permutation-null<br/>threshold, and the decision label. Wait for explicit human approval before<br/>Phase 2R. Phase 2R is expensive; do not pre-run any part of it.</p><hr/><h2>OUTPUT FORMAT (every report)</h2><p>Sections: Decision label / Frozen lineage / Gates / Method / Quantitative<br/>tables / Negative controls and failure cases / Mechanism explanation /<br/>Compute accounting / Evidence boundary.</p><p>Label every claim as exactly one of:<br/><code>observed fact</code> | <code>controlled intervention</code> | <code>interpretation</code> |<br/><code>untested hypothesis</code>.</p><p>State explicitly what the result does NOT license.</p>
